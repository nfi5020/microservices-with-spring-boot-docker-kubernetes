# Build the jar
# Maven
mvn clean build
# Gradle
gradle clean build --x test

# Run the application
mvn spring-boot:run

java -jar <name of the jar>

=============================================================================================
Spring Config documents: https://docs.spring.io/spring-cloud-config/reference/server/environment-repository/git-backend.html